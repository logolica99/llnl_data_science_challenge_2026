interface:
  display_name: "Hackathon NDE Pipeline"
  short_description: "Orchestrate seal-free Stage 0–3 via MCP subagents"
  default_prompt: "Use $hackathon-nde-pipeline to run metadata → registration → defect agents/CSVs → NDE report by dispatching stage subagents through segmentation-tools MCP only."

dependencies:
  tools:
    - type: "mcp"
      value: "segmentation-tools"
      description: "Required CT metadata, registration, seal-free metrics/defect, and reporting MCP tools"
      transport: "stdio"
