interface:
  display_name: "Strut Defect Analyzer"
  short_description: "Measure and classify hash-bound strut evidence"
  default_prompt: "Use $strut-defect-analyzer to measure Stage 2 struts or run the handoff-bound Stage 3 defect team without recomputing prior-stage science."

dependencies:
  tools:
    - type: "mcp"
      value: "segmentation-tools"
      description: "Required Stage 2 metrics and Stage 3 classification/evidence MCP tools"
      transport: "stdio"
