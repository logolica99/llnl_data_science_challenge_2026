interface:
  display_name: "NDE Report Generator"
  short_description: "Assemble graph-aware Stage 4 NDE reports"
  default_prompt: "Use $nde-report-generator to assemble the hash-verified Stage 4 lattice NDE report."

dependencies:
  tools:
    - type: "mcp"
      value: "segmentation-tools"
      description: "Required graph reporting, spatial statistics, and lattice rendering tools"
      transport: "stdio"
