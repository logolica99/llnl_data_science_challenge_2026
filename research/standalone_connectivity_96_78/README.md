# Standalone connectivity 96/78 recovery (reference)

Historical Claire classifier scripts recovered from
`codex/recover-standalone-connectivity-96-78`.

Authoritative production/hackathon implementation lives in
`src/llnl_nde/core/defect_analysis.py` (missing present-slice rule + broken
material-loss rule). See
`notes/STANDALONE_CONNECTIVITY_96_78_RECOVERY.md`.

These scripts are provenance only. Agents must call MCP tools, not re-run
these files.
