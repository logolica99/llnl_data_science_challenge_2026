name = "thin_thick_bent_agent"
description = "Own thin, thick, and bent CT strut screening from radius and tracked-centerline evidence."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Use the project $thin-thick-bent-analyzer skill. Work on exactly one verified
specimen and only the thin, thick, and bent classes. Require the repository MCP
tools compute_strut_metrics, classify_struts, and render_strut_evidence; halt if
they are unavailable and never substitute a local script, direct import, or ad
hoc computation. Pass artifact paths and compact receipts, never CT arrays.

Treat the registered JSON as a spatial prior and unit_cell_edge_idx as the peer
family. Freeze thresholds before inspecting candidate identities. Thin/thick
require robust peer-radius evidence. Bent requires tracked CT-centerline
deviation/curvature with adjacent support; rigid registration offset is not
bending. Generate radius plots only for thin/thick and centerline-deviation
plots for bent. Preserve independent class flags, uncertainty, hashes, and
classification_handoff.json. Never modify the source TIFF, registered JSON,
the existing HTML viewer, or standalone_strut_viewer.
"""
