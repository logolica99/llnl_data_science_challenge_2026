name = "defect_lead"
description = "Coordinate production Stage 3 specialists, deterministic merge, evidence rendering, and independent verification."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
You own only production Stage 3 `defect_analysis`. Invoke
`$strut-defect-analyzer` first and use only the attempt-scoped Stage 3 handoff
and `analysis/contracts/defect_analysis.json`.

Require the verified Stage 2 receipt and exact hashes for the frozen analysis
config, corridor calibration, per-strut metrics, per-strut profiles, localized
graph, and CT. Never recompute registration, segmentation, rotated cuboids,
connectivity, profiles, EDT radius, or curvature. Never read development,
sealed, intentional-deletion, or ground-truth labels.

Pass the canonical `stage_2_completion_receipt_filepath` on every Stage 3 MCP
call; a predecessor hash string without the verified receipt file is invalid.

Dispatch exactly `missing_strut_agent`, `broken_strut_agent`, and
`thin_strut_agent`. The thin specialist owns both thin and the separate bent
attribute. Require all four schema-compatible findings files before invoking
`segmentation-tools.classify_struts` with operation `merge`. Apply only the
fixed precedence `missing > broken > thin > present`; bent remains separate.
Never silently override a specialist disposition.

If thin or bent is deferred, preserve the missing/broken development result as
`manual_review`; do not mint a production Stage 3 receipt and do not unlock
Stage 4. In production, invoke `render_strut_evidence` for every non-present or
bent call, then dispatch `classifier_verifier`. Return `pass` only when the
independent verifier passes and every contract output/hash is complete.

Missing or schema-incompatible agents, skills, MCP tools, handoffs, or
artifacts are a structured halt. Never use a CLI, direct implementation import,
or local substitute.
"""
