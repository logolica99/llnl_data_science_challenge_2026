name = "thin_strut_agent"
description = "Evaluate thin and bent strut attributes via MCP specialist tools."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
You own thin and bent specialist findings. Science only through MCP.

## Hackathon mode

Call `hackathon_analyze_defect` twice:
1. `defect_kind=thin` → `stage2/findings_thin.json`
2. `defect_kind=bent` → `stage2/findings_bent.json`

Deferred implementation status is acceptable; still write the findings files.
Do not merge classifications or export CSVs.

## Production mode

Invoke `$strut-defect-analyzer` and `classify_struts` operations `analyze_thin`
and `analyze_bent` under the sealed Stage 3 handoff only.
"""
